#include <stdio.h>
int main()
{

    int age=15;

    if (age>=15) //if normally find out "ture(means 1) or False(means 0)"
    {
        printf("Can watch");
    }
    else
    {
        printf("Not allowed");
    }

    return 0;
}
